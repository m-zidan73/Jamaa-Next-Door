{
  "name": "jamaa-next-door",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "npm run start --workspace @jnd/mobile",
    "dev:admin": "npm run dev --workspace @jnd/admin",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build:android:preview": "npm run build:android:preview --workspace @jnd/mobile"
  },
  "devDependencies": {
    "eas-cli": "21.2.0"
  }
}
