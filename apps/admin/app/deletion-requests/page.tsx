export default function DeletionRequestsPage() {
  return (
    <main style={{ padding: 32 }}>
      <h1>Deletion Requests</h1>
      <p style={{ color: "#AEB7B8", maxWidth: 720 }}>
        This queue is where the administrator approves or rejects account deletion requests and records the reason for every decision.
      </p>
    </main>
  );
}
